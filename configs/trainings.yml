model:
  name: unsloth/Qwen2-0.5B-Instruct-bnb-4bit

dataset:
  max_samples: 500

training:
  output_dir: outputs
  batch_size: 1
  gradient_accumulation_steps: 4
  learning_rate: 2e-4
  max_steps: 60
  epochs : 3

lora:
  r: 8
  alpha: 16
  dropout: 0
